## 1. 基线与依赖

- [x] 1.1 运行并记录现有后端、前端测试及构建基线，确认失败项与本变更无关后再继续
- [x] 1.2 对照 Context7 结果和阿里云、LangChain、Chroma、Taro、微信官方文档锁定新增依赖与调用参数，并通过最小导入/类型检查验证版本兼容
- [x] 1.3 扩展后端配置和 `.env.example` 的非敏感占位项，验证真实密钥不会出现在 Git diff、日志或 API 响应中

## 2. MySQL 领域模型与持久化

- [x] 2.1 先编写 SQL 迁移结构测试，再新增知识库、文档、索引任务表及索引/外键，并验证迁移可重复执行且不修改现有表行为
- [x] 2.2 先编写 Pydantic 模型测试，再实现知识库、文档、任务状态、配额和安全错误模型，验证非法名称、状态和数量被拒绝
- [x] 2.3 先编写 Repository 单元/集成测试，再实现知识库 CRUD、所有权过滤、分页统计和配额原子校验，验证跨用户访问不泄露数据
- [x] 2.4 先编写任务状态机测试，再实现文档记录、任务创建/claim/阶段更新/失败/恢复和重新索引 Repository，验证重复执行幂等

## 3. 文件上传与文档解析

- [x] 3.1 先编写文件安全测试，再实现流式 10MB 限制、文件名清洗、扩展名/MIME/signature 校验、SHA-256 和根目录路径约束
- [x] 3.2 先编写解析器契约测试，再实现 PDF、DOCX、Markdown 解析和控制字符清理，验证加密/扫描 PDF 返回明确失败原因
- [x] 3.3 先编写旧版 DOC 成功及失败测试，再实现 Tika DOC 解析适配器和可选外部服务配置，验证缺少运行时不会影响其他格式
- [x] 3.4 先编写分块测试，再实现中文优先递归分块、稳定 chunk ID、页码/章节元数据和总字符/chunk 上限

## 4. Embedding 与 Chroma 向量索引

- [x] 4.1 先编写网关测试，再实现百炼 `text-embedding-v4` 批量 Embedding、超时/重试、响应条数和维度校验
- [x] 4.2 先编写向量存储测试，再实现 Chroma PersistentClient、用户 collection 命名、文档 upsert/query/delete 和知识库 metadata 过滤
- [x] 4.3 先编写检索测试，再实现 Top-K、相关度门槛、单文档命中上限和统一 RetrievedChunk 输出，验证跨用户与跨知识库隔离
- [x] 4.4 先编写执行器测试，再串联解析、分块、Embedding、Chroma 和 MySQL 阶段状态，验证成功、部分失败、重试、启动恢复及幂等清理

## 5. 知识库后端 API

- [x] 5.1 先编写鉴权和 CRUD API 测试，再实现知识库创建、列表、详情、修改和删除接口，验证 5 库配额及跨用户访问
- [x] 5.2 先编写上传 API 测试，再实现 multipart 文档上传和 HTTP 202 状态返回，验证 20 文档配额、10MB 限制和异常临时文件清理
- [x] 5.3 先编写文档查询/删除/重建 API 测试，再实现列表、详情、删除和 reindex，验证文件、向量和数据库补偿清理
- [x] 5.4 扩展 OpenAPI 和统一错误响应测试，验证响应不包含 storage_key、文件系统路径、collection 名称或用户敏感标识

## 6. 私有与混合出题

- [x] 6.1 先编写兼容性模型/API 测试，再给异步出题请求增加可选 source_scope 和 knowledge_base_ids，验证所有旧请求序列化与行为不变
- [x] 6.2 先编写私有来源转换测试，再把 RetrievedChunk 转为安全 EvidenceSource/ResearchResult，并验证文档名、页码/章节和稳定引用 ID
- [x] 6.3 先编写 private 模式服务测试，再接入知识库所有权校验、私有检索、证据门槛和 DeepSeek 出题，验证绝不调用 Tavily 或回退模型记忆
- [x] 6.4 先编写 mixed Agent 隐私测试，再实现“先私有检索、按需联网”的路由和 Tavily 查询清洗，验证私有片段与路径不会进入工具参数
- [x] 6.5 先编写引用约束和历史快照测试，再扩展题目来源校验、任务结果和 quiz session 持久化，验证每题有私有引用且删除知识库后历史仍可查看
- [x] 6.6 运行全部后端测试，确认现有文本、URL、联网研究、异步轮询、用户、XP、报告和历史测试无回归

## 7. Taro 知识库界面

- [x] 7.1 对照已确认 HTML 原型新增知识库 API 类型、客户端和上传封装，并通过前端单元测试验证 token、multipart 上传和错误映射
- [x] 7.2 实现知识库列表、新建和空状态页面，通过截图/微信开发者工具检查原生导航安全区、配额和响应式布局
- [x] 7.3 实现知识库详情、文档列表、上传校验、索引进度、失败重试和删除确认，通过组件测试验证轮询终止和页面恢复
- [x] 7.4 在“我的”页面加入资料库入口并保持现有用户信息、历史和匿名兼容，通过现有个人中心测试及手工导航验证
- [x] 7.5 在首页加入联网/私有/智能混合来源和知识库选择，验证未登录限制、无就绪资料提示及旧文本/URL入口不变
- [x] 7.6 扩展生成过程、结果确认、答题解析和历史详情的私有文档引用展示，验证不出现本地路径且来源类型清晰

## 8. 验收与交付

- [x] 8.1 运行后端全量 pytest、前端 test、typecheck 和微信小程序 build，修复本变更造成的所有失败并记录结果
- [x] 8.2 使用临时目录和测试账户执行 PDF、DOCX、Markdown、DOC 的离线建库烟雾测试，验证四种格式及失败提示；未配置真实 Key 时只跳过显式标记的在线用例
- [x] 8.3 使用真实百炼、DeepSeek 和 Tavily 配置执行 private 与 mixed 端到端烟雾测试，验证任务轮询、引用、隐私日志和历史快照
- [x] 8.4 在微信开发者工具按 12 屏原型完成视觉和交互验收，确认导航栏不遮挡、上传/轮询/重试/删除流程完整且核心闯关正常
- [x] 8.5 更新部署、环境变量、目录备份、迁移、DOC/Tika 运行时及故障排查文档，并验证新环境可按文档完成配置
