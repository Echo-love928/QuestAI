## 1. 基线与依赖兼容

- [x] 1.1 在不改代码的状态下运行完整后端 pytest、前端测试、TypeScript 检查和微信小程序构建，记录基线并确认现有生成、答题、报告、登录、XP 和历史测试全部通过。（基线：后端 45 passed/2 skipped；前端 11 passed；TypeScript 与 `build:weapp` 通过。）
- [x] 1.2 先编写最小兼容性测试，覆盖 LangChain `create_agent`、`ChatDeepSeek` 工具调用、现有结构化题目输出、`TavilySearch` 和 `TavilyExtract` 原生异步调用，再升级并锁定一组全部通过的 LangChain 1.x 依赖版本。（已锁定并验证 LangChain 1.4.0 / Core 1.6.2 / DeepSeek 1.1.0 / Tavily 0.2.18。）
- [x] 1.3 增加 `TAVILY_API_KEY`、功能开关、研究总超时、单工具超时、工具调用数、搜索次数、结果数、提取 URL 数和证据 Token/字符预算配置，在 `.env.example` 提供无敏感信息示例，并通过配置单元测试验证缺失或越界配置产生明确错误。（真实 Key 仅写入被 Git 忽略的 `backend/.env`；配置测试 10 passed。）

## 2. 研究 Agent 与 Tavily 工具（后端 TDD）

- [x] 2.1 先为主题文本、完整材料、时效材料、合法 URL、无效 URL 和混合输入编写失败测试，再实现输入来源分类与后端 URL 基础校验，验证文本核心路径保持兼容。
- [x] 2.2 先为简单、复杂、新颖、歧义和时效主题的参数决策编写失败测试，再定义研究 Agent Prompt 与结构化 `ResearchBrief`，验证 Agent 能结束、请求搜索、请求提取或请求 Search→Extract。
- [x] 2.3 先为搜索深度、结果数、片段数、主题、时间范围、国家、语言和参数越界编写失败测试，再实现使用官方 `TavilySearch` 的 `tavily_search` 受控适配器，验证每次调用配置可动态变化且不暴露 API Key。
- [x] 2.4 先为单/多 URL、提取深度、相关查询、部分失败、全部失败和超时编写失败测试，再实现使用官方 `TavilyExtract` 的 `tavily_extract` 受控适配器，验证最多处理允许数量的唯一 URL 并保留失败信息。
- [x] 2.5 先为中文概念英文别名、全球主题、国家主题、城市写入查询词和中文查询保留英文官方资料编写失败测试，再实现语言与地域查询增强，验证不使用不存在的城市参数且默认不严格过滤语言。
- [x] 2.6 先为 localhost、环回、私网、链路本地、保留地址、云元数据地址、嵌入式凭据、危险协议和重定向目标编写失败测试，再实现 URL 安全策略，验证危险 URL 永远不会进入 Tavily 调用。
- [x] 2.7 先为总调用 4 次、Search 2 次、全程 Extract 3 个 URL、总超时和正文预算编写失败测试，再实现 Agent middleware 与累计预算，验证拆分调用不能绕过上限且达到预算后 Agent 必须停止。
- [x] 2.8 增加工具超时、限流、上游异常及连续失败测试，验证 middleware 向 Agent 返回安全的工具错误、服务层生成稳定领域错误，日志不包含 Key、完整输入或网页正文。

## 3. 证据与题库生成（后端 TDD）

- [x] 3.1 先为搜索摘要、用户 URL 提取、搜索结果提取、合法 URL、站点提取、规范化去重、稳定来源 ID、单条/总长度裁剪编写失败测试，再实现证据归一化器，验证公共来源模型不包含完整网页正文。
- [x] 3.2 先为第一方单来源、双站点相互支持、资料冲突、同名歧义和关键事实无来源编写失败测试，再实现 `ResearchBrief` 与服务层证据充分性校验。
- [x] 3.3 增加包含提示注入文字、伪造来源 ID 和工具指令的恶意资料测试，验证 Agent 与出题 Prompt 始终把网页视为不可信数据且无法改变工具权限、题型或输出 Schema。
- [x] 3.4 先扩展题库模型测试，覆盖 `grounding_mode` 的 `user_content`、`web_search`、`url_extract`、`mixed` 模式、`researched_at`、来源获取方式和每题 `source_ids`，再以可选新增字段实现模型，验证旧题库 JSON 仍能解析。
- [x] 3.5 先为用户原文和网络证据模式编写 Prompt 契约失败测试，再实现独立、版本化的结构化出题 Prompt，验证 Agent 研究输出经过校验后才会传给现有 DeepSeek 出题 Gateway。
- [x] 3.6 先为未知来源 ID、联网题目无引用、题量错误、模型空响应和重试耗尽编写失败测试，再扩展题库校验，验证无效题库不会返回前端且现有题型契约不变。
- [x] 3.7 先为文本、URL、Search→Extract 和完整原文四条流水线编写服务测试，再接入 `QuizService`，验证资料不足或危险 URL 时最终出题 Gateway 调用次数为零。
- [x] 3.8 先为研究不可用、搜索不可用、提取不可用、危险 URL、证据不足、主题歧义、预算耗尽和 AI 出题失败编写 API 测试，再实现稳定业务码与安全文案，验证 HTTP 状态、统一响应结构和现有成功响应字段兼容。
- [x] 3.9 为生成接口增加登录用户/匿名客户端限流测试并实现成本保护，验证超过阈值不会调用 Tavily 或 DeepSeek，且不会改变微信登录接口现有限流行为。

## 4. 来源快照与历史兼容（数据库 TDD）

- [x] 4.1 编写独立、可重复执行的 MySQL 增量 SQL，为 `quiz_sessions` 增加可空依据模式、来源 JSON 和检索时间，并在测试库连续执行两次验证幂等性。
- [x] 4.2 同步初始化 SQL 的最终表结构，并通过全新测试数据库初始化测试验证用户、题库、答题记录和报告表均可创建。
- [x] 4.3 先扩展 MySQL 仓储测试，覆盖新来源快照写入/读取、题目来源关联和旧空字段，再实现仓储映射，验证既有历史记录无需回填且仍能正常读取。
- [x] 4.4 运行数据库与用户系统全部测试，验证新增来源持久化不改变题库保存失败的尽力而为语义、报告幂等性、XP 累加和用户历史分页。（含真实 MySQL 集成：109 passed。）

## 5. URL 与来源 UI 原型人工确认

- [x] 5.1 严格沿用现有黄色/橙色漫画练习册视觉更新 `prototype/01-core-flow.html`，启用 URL 输入并补充搜索中、提取中、资料核对、URL 错误、证据不足、歧义、预览来源及答题来源状态，通过桌面三列和手机单列预览验证布局。
- [x] 5.2 生成所有新增状态的预览图并提交人工审核；只有用户明确确认后才开始修改 Taro 页面，以审核结论作为本任务完成证明。（用户已于 2026-09-09 明确确认通过。）

## 6. 小程序前端实现与测试

- [x] 6.1 先扩展 TypeScript 契约和本地缓存兼容测试，再加入输入来源类型、四种依据模式、资料来源与题目引用类型，验证升级前缓存不会导致首页、预览或答题页白屏。
- [x] 6.2 先为 URL 标签切换、格式校验、文本/URL 请求体和输入保留编写失败测试，再按确认原型启用 URL 入口，验证无效 URL 不发请求且文本入口行为不变。
- [x] 6.3 先编写请求取消和竞态测试，再把生成请求封装为可取消的 Taro `RequestTask`，验证取消或页面卸载后旧响应不会导航或覆盖新请求。
- [x] 6.4 按确认原型实现搜索、提取、核对阶段及分类错误操作，通过页面测试验证原输入始终保留，重新获取资料、检查网址和返回修改均可操作。
- [x] 6.5 按确认原型实现可复用来源组件并接入闯关预览、答题讲解和历史详情，通过组件测试验证本题来源过滤、HTTPS 打开/复制、获取方式、用户原文模式和旧历史缺失来源状态。
- [x] 6.6 运行前端全部测试、TypeScript 检查和 `build:weapp`，并在微信开发者工具中验证原型视觉、原生导航栏安全区、URL 输入、请求取消和来源链接交互。（17 项前端测试、类型检查和 Taro 4.2.1 构建通过；微信模拟器确认首页、安全区、URL 切换及合法输入状态正常，请求取消和来源交互由组件测试覆盖。）

## 7. 端到端验收与发布保护

- [x] 7.1 使用伪造 Tavily 与 DeepSeek 跑通匿名和登录用户的“文本/URL -> Agent 研究 -> 出题 -> 答题 -> 报告 -> 历史详情”，验证现有核心流程与新增来源字段同时工作。（新增组合 API 测试，验证匿名 URL 不入库、登录文本题库/报告/来源进入历史详情。）
- [x] 7.2 使用确定性伪造工具验证简单主题只搜索、复杂主题 Search→Extract、URL 直接 Extract、URL 后补充 Search、证据充分后停止和达到预算强制停止等工具序列。（研究 Agent、工具适配器与预算测试均通过。）
- [x] 7.3 在配置测试 Tavily Key 后执行受控在线烟雾测试，覆盖 Harness Engineering、新近版本、简单稳定概念、中文/英文技术词、全球主题、城市主题、有效 URL、不可提取 URL 和同名歧义，并人工核对题目与来源一致。（真实 Tavily + DeepSeek 已覆盖 9 个场景；成功题目引用均有效，歧义按预期拦截。）
- [x] 7.4 验证搜索/提取超时、部分提取失败、额度限流、DeepSeek 空响应、恶意 URL、提示注入和数据库暂时不可用时的恢复，确认系统不会静默退回模型旧知识。（后端故障矩阵与 API 测试通过；404 URL 仅使用重新取得的网络证据恢复。）
- [x] 7.5 汇总工具调用序列、搜索次数、提取 URL 数、有效来源数、裁剪量、阶段耗时和失败分类日志，确认不含 API Key、完整敏感 URL、完整用户输入或网页正文，并形成启用、限流和回滚检查清单。（见 `docs/联网出题验收记录.md`。）
- [x] 7.6 再次运行完整后端 pytest、前端测试、类型检查和微信小程序构建，对比基线并确认生成、答题、XP、报告、登录、资料编辑和历史功能无回归。（后端 113 passed/3 skipped；前端 17 passed；TypeScript 与 Taro 4.2.1 微信构建通过。）
