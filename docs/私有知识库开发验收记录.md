# 私有知识库开发验收记录

## 变更前基线（2026-09-10）

- 后端：`backend/.venv/Scripts/python.exe -m pytest`，122 passed，3 skipped。
- 前端单元测试：`npm test`，19 passed。
- 前端类型检查：`npm run typecheck`，通过。
- 微信小程序构建：`npm run build:weapp`，通过。
- 备注：曾并发启动两个 Taro build，第二个进程清理同一个 `dist` 时出现 `ENOENT app-origin.wxss`；改为单进程顺序构建后通过，属于构建目录并发竞争，不是产品代码基线失败。

## 最终验收

- 后端全量测试：179 passed，10 skipped；跳过项为显式 opt-in 的 MySQL 与在线外部 API 测试。
- MySQL 集成：知识库 Repository 与历史来源快照共 8 项通过。
- 文档解析：Markdown、真实文本 PDF、真实最小 DOCX、DOC 适配器、扫描/加密 PDF 失败提示共 8 项通过。
- 百炼在线烟雾：成功返回 2 条 `text-embedding-v4` 向量，每条 1024 维；未记录输入和密钥。
- 前端测试：24 项通过。
- TypeScript：`tsc --noEmit` 通过。
- 微信小程序构建：Taro 4.2.1 编译成功。
- DOC/Tika 实机烟雾：本机 Word 生成旧版 DOC，Tika 3.3.2 成功读取 70 个字符并命中预期内容；临时 DOC 已安全删除。
- 微信开发者工具：使用真实 AppID、IDE 服务端口 27083 和自动化端口 9420 完成运行态验收；按 12 屏原型逐项核对，并自动留存知识库列表、详情、创建、上传、首页来源选择和“我的”共 6 张关键页面截图。
- DeepSeek 私有 RAG 在线烟雾：生成 3 道题，1 个私有来源，每题均引用私有来源。
- Tavily + DeepSeek 智能混合在线烟雾：生成 3 道题，包含 1 个私有来源和 7 个公开来源，每题均保留私有依据。
- 真实建库链路：自动创建临时知识库、上传 Markdown、等待解析/分块/百炼向量化至 ready，确认列表统计、详情状态和首页私有来源选择正确，随后通过删除接口清理临时知识库、文件与向量。
- 运行态回归修复：验收发现复用的 MySQL 连接可能保留旧读事务，导致上传后查询不到刚创建的文档任务并返回 500；已在通用只读查询前后结束残留事务，并增加回归测试。真实上传复测通过。
- 文件限制：后端默认配置、前端拦截、环境变量示例、原型和 OpenSpec 已统一为单文件不超过 10MB。
- 已知非阻断项：Chroma 在 Python 3.14 下产生一条 `asyncio.iscoroutinefunction` 弃用警告，不影响当前功能和测试结果。

## 微信开发者工具预览证据

- `prototype/previews/knowledge-list-runtime.png`
- `prototype/previews/knowledge-detail-runtime.png`
- `prototype/previews/knowledge-create-runtime.png`
- `prototype/previews/knowledge-upload-runtime.png`
- `prototype/previews/knowledge-source-runtime.png`
- `prototype/previews/knowledge-profile-runtime.png`
